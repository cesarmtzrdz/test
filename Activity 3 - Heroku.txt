+++++ INstall Heroku CLI
wget -qO- https://toolbelt.heroku.com/install-ubuntu.sh | sh

+++verify CLI install
heroku version

++++to deply spring music to heroku we need to add the app build script
, we go to build.gradle and below the //Spring Cloud Connectos section
we add a new line:
compile "org.springframework.cloud:spring-cloud-heroku-connector:1.2.3.RELEASE"

++++agrega a git los archivos:
git add *
git commit -m "added heroku connector"

++++++ login to kerou
heroku login

+++ createa app in hero
heroku create csmrmusic

+++ prvision a pstgress db
heroku addons:create heroku-postgresql:hobby-dev

+++push code to remote heroku git repo
git push heroku master


+++login to keroku open app frmo dashboard

+++explore:
heroku logs
heroku addons:services