+++++ Connecto to VM

ssh user@host  dockerlab@dockeracademy10.eastus2.cloudapp.azure.com  PaaS2017

+++++ install docker to vm
sudo apt-get update

+++install the curl package
sudo apt-get install curl

++install docker
curl -ssl https://get.docker.com/ | sh

++++ verify docker install
sudo docker version

+++clone sameple porjec from github
git clone https://github.com/paasacademy/docker-activity-base.git

+++change dir
cd docker-activity-base


++++ create docker virtual network
sudo docker network create --driver bridge paasacademy

++verify networkf
sudo docker network ls


++++ startup mongodb container
sudo docker run --net=paasacademy --name mongodemo -d mongo

++verify container runing
sudo docker ps


+++ verify mongodb network isolation
sudo docker network inspect paasacademy

+++++ build a  custom docket image for the app
sudo docker build -t csmrapp .

++ verify image created
sudo docker images

+++ run the custom built TODO app docker image and conenct it to paaasacademy net
sudo docker run --net=paasacademy -p 80:80 -d --name dockerdemo csmrapp

+++verify container is running
sudo docker ps

++verify dockerdemo os connecto to paasacademy
sudo docker network inspect paasacademy


+++++++++ INSTAll docker compose
sudo -i
curl -L https://github.com/docker/compose/releases/download/1.8.0/docker-compose-`uname -s`-`uname -m` > /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
exit

sudo docker-compose version


+++++ running docker-compose
sudo docker-compose -p PaaSDemo up -d

++verify mongo and csmrapp
sudo docker ps


